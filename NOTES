This minor release fixes a race with devd and a bug in geli detaching that
causes the detacher to eat a core and block the log.

The devd configuration was updated, please run the following command
immediately after completing the installation procedure:

	service devd restart

